# nflow-ayuda-psicologica
# Inicializar git si no está inicializado
git init

# Configurar el usuario de git (reemplaza con tus datos)
git config user.name "NFlow"
git config user.email ""rmportbou@gmail.com

# Añadir todos los archivos al staging
git add .

# Hacer el primer commit
git commit -m "Commit inicial del proyecto NFlow"
